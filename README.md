# How To Run
```bash
    bash path/to/run.bash -c "..." -t "..."
```
# Input
```bash
    -c: command
    -t: the start time of that command
```
# Output
```bash
    file log.logs # log.logs located in pwd
```

